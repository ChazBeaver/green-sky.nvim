# green-sky.nvim

`green-sky.nvim` is the Neovim colorscheme for the broader **Green-Sky** theme ecosystem.

This repo contains the Neovim theme itself. It is intended to pair with the Omarchy theme repo:

- **Omarchy theme repo:** `omarchy-green-sky-theme`
- **Neovim theme repo:** `green-sky.nvim`

## What this repo is

This repo is focused on Neovim only.

It provides the editor colorscheme, highlight groups, and plugin styling needed for Neovim to match the rest of the Green-Sky theme.

## Relationship to Omarchy

`green-sky.nvim` is the editor-side counterpart to `omarchy-green-sky-theme`.

That means:

- `green-sky.nvim` handles Neovim colors and highlights
- `omarchy-green-sky-theme` handles desktop and app theming for Omarchy
- both are meant to share the same palette and visual direction

## Theme pairing

Use these together for a matching setup:

- **Neovim:** `green-sky.nvim`
- **Omarchy:** `omarchy-green-sky-theme`

## Goal

The goal of `green-sky.nvim` is to make Neovim feel like a natural part of the wider Green-Sky environment instead of a separate, mismatched editor theme.
